# handwritten-digit-classification
This project explores a fundamental machine learning task: classifying handwritten digits (0–9) using the Scikit-learn Digits dataset. By processing these grayscale images, the notebook implements and compares two distinct algorithms-Logistic Regression and K-Nearest Neighbors (KNN)—to evaluate their accuracy and predictive performance.
